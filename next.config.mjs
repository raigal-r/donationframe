/** @type {import('next').NextConfig} */
const nextConfig = {
    // webpack(config, options) {
    //     // Add wasm extension
    //     config.experiments = {
    //       ...config.experiments,
    //       asyncWebAssembly: true,
    //     };
    
    //     // Adds a rule for WebAssembly files
    //     config.module.rules.push({
    //       test: /\.wasm$/,
    //       type: 'webassembly/async',
    //     });
    
    //     return config;
    //   },
}

export default nextConfig
